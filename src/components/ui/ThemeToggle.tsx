export default function ThemeToggle() {
  // In this brutalist design the color system is fixed (black + cream).
  // The toggle is kept as a noop placeholder so the import doesn't break.
  return null
}
